<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:admarvel="http://schemas.android.com/apk/res-auto"
    android:id="@+id/rl_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="${packageName}.${activityClass}"
    android:background="@drawable/lockscreen_background">

    <ImageView
        android:id="@+id/logo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:padding="@dimen/padding_splash_app_logo"
        android:src="@mipmap/ic_launcher" />

    <FrameLayout android:id="@+id/adMediatorContainer"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"/>

    <in.lockerapplication.views.CircularRing
        android:id="@+id/cr_lock_pattern"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:text="@string/abs__action_bar_home_description" />

    <in.lockerapplication.views.CustomDigitalClock
        android:id="@+id/time_textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="20dp"
        android:shadowColor="@color/shadow_color"
        android:shadowDx="2"
        android:shadowRadius="2"
        android:textColor="@android:color/white" />

</RelativeLayout>